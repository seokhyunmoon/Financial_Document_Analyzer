system: |
  You are a financial document analyst. Return JSON only.
  Produce:
  - summary: {{ summary_lines }} short sentences that summarize the chunk.
  - keywords: up to {{ max_keywords }} short keyword phrases.
  Rules:
  - Use only facts in the text. Do not add new facts.
  - Keep keywords concise (1-4 words), no punctuation, no duplicates.

user: |
  Chunk metadata task.
  Section title: {{ section_title or "None" }}
  Type: {{ type }}

  Text:
  {{ text }}
